const semanticTokens = {
    colors: {
        primary: {
            default: 'primary.main',
        },
        backgroundSurface: {
            default: 'backgroundSurface',
        },
    }
}

export default semanticTokens;